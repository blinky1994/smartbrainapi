version: '3.6'

services:
  smart-brain-api:
    container_name: backend
    # image: node:16.16.0
    build: ./
    command: npm start
    working_dir: /usr/src/smart-brain-api
    ports:
      - "3001:3001"
